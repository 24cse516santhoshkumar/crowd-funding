spring.datasource.url=jdbc:h2:file:~/crowdfundingdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=SA
spring.datasource.password=
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.format_sql=true

# Server
server.port=${PORT:8080}

# OAuth2 Configuration (Commented out for now to fix login issues)
# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:your-google-client-id}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:your-google-client-secret}
# spring.security.oauth2.client.registration.google.scope=openid,email,profile

# spring.security.oauth2.client.registration.apple.client-id=${APPLE_CLIENT_ID:your-apple-client-id}
# spring.security.oauth2.client.registration.apple.client-secret=${APPLE_CLIENT_SECRET:your-apple-client-secret}
# spring.security.oauth2.client.registration.apple.scope=openid,email,name

# Email Configuration (Commented out for now to fix login issues)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${EMAIL_USERNAME:your-email@gmail.com}
# spring.mail.password=${EMAIL_PASSWORD:your-app-password}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
jwt.secret=${JWT_SECRET:your-super-secret-jwt-key-change-this-in-production}
jwt.expiration=${JWT_EXPIRATION:86400000}

# App Configuration
app.frontend-url=${FRONTEND_URL:http://localhost:8081}
app.reset-password-url=${RESET_PASSWORD_URL:http://localhost:8081/reset-password}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:${app.frontend-url}}
